# 개발 인스트럭션

이 프로젝트는 Python으로 개발합니다. 아래 컨벤션을 따르되, 형식만을 위한 과도한 수정으로 생산성을 떨어뜨리지 않습니다. 특히 명명 규칙과 들여쓰기·개행을 우선 지킵니다. 중괄호를 쓰는 다른 언어를 추가할 때는 해당 언어의 개행 관례를 따릅니다.

## 클래스와 함수 설명

- 모든 클래스와 함수에 삼중 큰따옴표(`"""..."""`)로 docstring을 작성합니다.
- 각 클래스·함수의 역할과 다른 코드와의 관계를 설명합니다. 설명할 맥락이 적으면 한 문장으로 작성해도 됩니다.
- 코드의 모든 줄에 주석을 달 필요는 없습니다.
- 같은 줄에 주석을 쓸 때는 코드에서 최소 두 칸 띄우고, `#` 뒤에는 한 칸 띄웁니다.

## Python 명명 규칙

- 변수와 함수: `snake_case` (`user_name`, `calculate_total`)
- 클래스: `PascalCase` 또는 `CapWords` (`UserProfile`)
- 상수: `SNAKE_CASE` (`MAX_LIMIT`)
- 모듈: 짧은 소문자 이름, 필요하면 언더스코어 사용 (`account_entry`)
- 패키지: 짧은 소문자 이름을 권장하며 여러 단어에는 언더스코어를 사용 (`smartbudget_server`)
- 내부용·비공개 멤버: 앞에 언더스코어 하나 사용 (`_internal_value`, `_private_method`)

## 들여쓰기와 공백

- 탭 대신 스페이스 4칸으로 들여씁니다.
- 최상위 함수·클래스 정의 사이에는 빈 줄 두 줄을 둡니다. 클래스 내부 메서드 정의 사이에는 빈 줄 한 줄을 둡니다.
- 대입·비교·논리 연산자 양옆에는 공백 한 칸을 둡니다 (`x = a + b`, `a == b`, `a and b`).
- 함수 정의와 호출의 키워드 인자 `=` 양옆에는 공백을 두지 않습니다 (`def func(default_val=10):`).
- 괄호 바로 안쪽에는 공백을 두지 않습니다 (`my_list[0]`).

## 임포트

임포트는 파일 상단에 두고, 표준 라이브러리, 서드파티 라이브러리, 프로젝트 내부 모듈 순으로 배치합니다. 그룹 사이에는 빈 줄 한 줄을 둡니다.

```python
import os
import sys

import numpy as np

import my_local_module
```

## 다른 언어

여기서 다루지 않은 언어는 해당 언어의 공식 컨벤션 가이드라인을 따릅니다. 공식 가이드가 없다면 널리 통용되는 컨벤션을 따릅니다.

## 작업 전 문서 확인

- 작업 시작 전 `docs/index.md`를 읽습니다.
- index의 조건에 따라 작업 범위와 일치하는 문서를 읽습니다. 모든 문서를 매번 읽을 필요는 없습니다.
- 작업 결과로 문서 갱신이 필요하면 해당 문서를 갱신하고 index의 등록 정보도 맞춥니다.

## 작업 완료 전 검증

- 프로젝트 파일을 변경한 작업은 완료 전 `.codex/hooks.json`의 종료 훅을 통해 Ruff 검사, pytest, 별도 Codex 검토를 통과해야 합니다.
- Ruff 포맷팅과 기본 안전 수정은 자동 적용합니다. 자동 수정으로 해결되지 않는 오류, 테스트 실패, AI 검토 실패는 완료라고 주장하지 말고 사용자에게 구체적으로 보고합니다.
- 검토 모델은 `gpt-5.6-luna`입니다. `docs/` 문서 사이의 모순, 코드 변경의 문서 반영 필요 여부와 반영 상태, 변경된 클래스·함수 docstring의 역할·맥락 설명을 검토합니다.
- 최종 답변에는 실행된 검증 결과와 남은 실패·미검증 항목을 명시합니다. 훅이 실행되지 않았다면 검증이 끝난 것처럼 표현하지 않습니다.
- 하네스 설정과 실행 방법은 `docs/harness.md`를 따릅니다.
